# Dev-environment defaults (committed, per Symfony's .env.$APP_ENV convention). APP_SECRET
# used to live here (a real generated value, not a placeholder) - moved to .env.dev.local
# (gitignored via web/.env.*.local) since it's a real secret, not a shareable default.
